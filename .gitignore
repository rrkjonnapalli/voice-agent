# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
recordings/
use_*

# local dev TLS cert -- self-signed, machine-specific (bakes in a LAN IP), never commit a private key
key.pem
cert.pem